Можно вывести значение на страну , а не в консоль

    docoment.write(`<div>
        <h3> ${products[0].name}</h3>
        <p>${productsp[0].price}</p>
                    </div>`);
    
        Back Quote
 `` `` `` `` `` `` `` ``
 `` - позволяют внутри строк вставлять переменные (внутри html текста)
 `<h2>${movies.title}</h2>`
 
{} - интерполяция 

Работает только внутри `${}`

